import React from 'react';

const MyItems = ({item}) => {
    const { _id, name, img, description, price, quantity, supplier } =
      item;
    return (

        <div>
            
        </div>
    );
};

export default MyItems;